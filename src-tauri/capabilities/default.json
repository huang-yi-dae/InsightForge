{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "InsightForge 桌面端默认权限：主窗口 + 用系统默认程序打开外部链接。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
